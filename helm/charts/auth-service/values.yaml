replicaCount: 2

image:
  repository: ""  # Set by CI
  tag: ""         # Set by CI
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
